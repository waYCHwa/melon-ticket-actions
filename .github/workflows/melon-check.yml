name: Melon Ticket Checker

on:
  schedule:
    - cron: '*/5 * * * *' # 每5分鐘
  workflow_dispatch:

jobs:
  job:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Check Tickets
        id: check
        uses: mooyoul/melon-ticket-actions@v1.1.0
        with:
          product-id: 212702
          schedule-id: 100003
          seat-id: 1_0
          # 不用 Slack
          message: '🎟️ 有票了快搶！'

      - name: Send Discord Notification
        if: success()
        run: |
          curl -H "Content-Type: application/json" \
          -X POST \
          -d '{
            "content": "🎟️ Melon Ticket 可能有票了！快去查看！"
          }' \
          ${{ secrets.DISCORD_WEBHOOK }}
